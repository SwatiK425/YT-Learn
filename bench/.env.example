# bench/.env.example — copy to bench/.env and fill in. NEVER commit .env.
# Same model + key are sent to BOTH baseline and candidate for fair A/B.
ZEN_API_KEY=sk-your-opencode-zen-key-here
MODEL=deepseek-v4-flash-free
PROVIDER=opencode-zen
